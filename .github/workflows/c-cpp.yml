name: C/C++ CI -Build status

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: x86_64-pc-linux-gnu

    steps:
    - uses: actions/checkout@v3
    - name: configure
      run: make -c 3_Implementation
